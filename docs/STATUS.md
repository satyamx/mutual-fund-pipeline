# STATUS — MF Analysis (session handoff)

Repo-tracked mirror of the build status, so a `git push` hands off the full picture without relying on `~/.claude` memory syncing. See `CLAUDE.md` for orientation and the honesty invariant; deeper design rationale is in project memory (`resume-point.md`, `mf-architecture-decisions.md`) if that syncs to your environment.

**As of 2026-07-25: MF repo HEAD = `849752c`, pushed and in sync with `origin` (`github.com/satyamx/mutual-fund-pipeline`) — the MF repo HAS a remote, and it is also the CI deploy path (GitHub Actions). Hisaab Kitaab repo HEAD = `9885565` (clean, still local-only).**

> **Two standing traps for any new session.** (1) `git fetch` before trusting ANY status: on 2026-07-25 local `master` was found 9 commits behind, with an entire session's work invisible. (2) `mf_cache/` is gitignored AND restored in CI from `actions/cache` — never put unbackfillable work there. `ledger/` and `overrides/` live outside it for exactly that reason, and the universe manifest now lives there too (`overrides/universe_manifest.csv`, moved 2026-07-25).

## Product shape (current)
- **System A (live, `mf_agent_orchestrator.py` → `RecommendationEngine`)** = the honest SCREEN: raw NAV facts (CAGR/vol/maxDD/Sortino/excess-vs-benchmark) + profile-weighted sub-scores + a **tri-colour 🟢BUY / 🔵HOLD / 🔴SELL verdict**. The verdict is a **transparent RULE over colour-coded metrics + hard compliance gates**, NOT the old below-chance weighted composite (which is deleted). The weighted "screen score" (0–100) is shown only as a banded *supporting datapoint*. Now ALSO carries a `cohort_signal` (see below) as a second, separate supporting datapoint — never folded into the verdict rule.
- **System B ("Sentinel", `mf_sentinel.py` → `SentinelEngine`)** = BUILT + wired into `MasterOrchestrator.evaluate()`/`print_report()`. Typed compliance/factor/manager ALERTS (INFO/WATCH/HIGH, each with an `AlertBasis` and evidence) + NFO manager-proxy dossier via `NFOAssessor`. Emits alerts/dossiers, never a fund number. `ScoringEngine.FACTOR_MAP` in `mf_pipeline.py` is kept (not deleted) as research-only infra — `mf_factor_backtest.py` still needs it live to regenerate the AUC~0.46 guardrail number Sentinel's `GUARDRAIL_ONLY` alerts cite; the live orchestrator never imports `ScoringEngine`.
- **Live cohort scoring (`mf_live_score.py` → `score_live()`)** = BUILT + wired into `MasterOrchestrator.evaluate()` this session (to-do #4 + the remainder of #1). Scores the trained `cohort_q1` elastic-net model at ONE anchor (the target fund's own latest NAV date, capped at the orchestrator's frozen `TODAY`) instead of the full 144-anchor training grid — reusing `mf_features.assemble_panel` verbatim over a one-anchor, universe-wide panel (NOT cohort-restricted — see module docstring for why that would silently skew the z-score inputs). Honesty gates: `NOT_IN_UNIVERSE` / `STALE_NAV` (>30d) / `THIN_COHORT` (<4 live cohort peers) always beat a number; `OK` carries `probability` + `cohort_percentile` + `signal_context` (holdout AUC/base rate) + `imputed_features`, always labelled a weak supporting datapoint. Verified via `--selftest`: the live one-anchor panel reproduces `features.parquet` exactly at a historical anchor (feature parity), predictions match the shipped artifact bit-for-bit (prediction parity), and hard-truncating post-anchor NAV data doesn't change the live panel (anti-lookahead on the live code path specifically, on top of `mf_features.py`'s own selftest). Isolated in its own try/except in `evaluate()` — a bug here degrades to a coverage flag, never discards an otherwise-complete evaluation.
- Honesty invariant: HOLD is the default under thin evidence; a hard compliance breach forces SELL; BUY carries a "manager skill & holdings unverified" caveat; missing data = coverage flag, never a pass. The only OOS-validated signal is the within-cohort `cohort_q1` model (holdout AUC ~0.578, weak).

## Verdict rule + coloring thresholds (as implemented, `RecommendationEngine.run`)
Metric coloring `_band(x, good, bad, higher_better)` → green/red/amber, grey if n/a:
- `cagr` ≥0.10 green / ≤0.06 red · `excess_cagr_3y` ≥+0.01 / ≤−0.01 · `sortino_3y` ≥0.75 / ≤0.0 · `max_dd_3y` ≥−0.20 / ≤−0.35 (shallower better) · `consistency` ≥0.60 / ≤0.40 · `expense` ≤0.010 green / ≥0.020 red (lower better) · `compliance` = red if any critical, amber if warnings or (active & holdings missing), else green · `screen_score` ≥65 green / <45 red / else blue.
- Core metrics counted = [cagr, excess_cagr_3y, sortino_3y, max_dd_3y, consistency].
- **Rule:** `SELL` if (any critical breach OR any regulatory red_flag OR reds≥3 OR (reds>greens AND screen_score red)); else `BUY` if (greens≥3 AND reds==0 AND screen_score≠red); else `HOLD`.
- Return adds: `verdict, verdict_color, verdict_caveat, metric_colors`. Thresholds are TUNABLE defaults — revisit once the cohort percentile is wired into the verdict.

## Done this session (MF commits)
- `b44a029` — `build_cohort_artifact` (mf_model.py) writes the FULL cohort inference payload → `mf_cache/phase_b/model_artifact_cohort.json`; NEW `mf_infer.py` scores a live fund with numpy alone (no sklearn). `python mf_infer.py --selftest` proves numpy == sklearn to 2e-14.
- `31d42a2` — killed the surfaced composite; `RecommendationEngine` returns an honest SCREEN (facts + sub-scores + `[DATA GAP]` flags); `ProfileRiskScorerAgent` now returns a `facts` dict; compliance inflation gone (no numeric compliance score). print_report rewritten.
- `29a633a` — restored the tri-colour verdict honestly (rule over coloured metrics); fixed a real bug — regulatory red-flag matcher used naive substring ("ban" fired on "bank margins" → spurious SELL), now word-boundary matched.
- `4c11ce6` — added `CLAUDE.md`.
- **NEW `mf_sentinel.py`** (System B, item #3) + wiring into `mf_agent_orchestrator.py` (`MasterOrchestrator.sentinel`, `evaluate()` returns `sentinel=`, `print_report()` prints alerts+NFO dossier). Multi-angle code review before commit surfaced and fixed 8 real issues: `EQ_DRAWDOWN_DEEP` always emitted INFO (copy-paste — deep tier now WATCH); `SEBI_CATEGORY_UNKNOWN` (a coverage gap) was mapped to WATCH/REGULATORY instead of INFO/COVERAGE — a genuine honesty-invariant violation, now fixed + selftest-locked; passive-fund expense INFO threshold was dead code (== the WATCH threshold, no `PASSIVE_EXPENSE_INFO` const existed); `NFO_MANAGER_PROXY_UNAVAILABLE` fired unconditionally because `self._managers_df` was never threaded into `_nfo()` (now wired via new `_manager_prior_funds()` helper); legacy Solution-Oriented bucket had no branch in `_factor_alerts` (silently produced zero alerts/zero dormant, breaking the module's own fire-or-dormant invariant); hand-rolled drawdown math replaced with the existing safe-divide-guarded `QuantEngine.max_drawdown`; dead `dossier` param removed from `_manager_alerts`; **known limitation** — `gate.assess()` needs `allotment_date` for NEWBORN/YOUNG gating but `FundDossier` has no such field; worked around by deriving it from `nav.dropna().index.min()` (a good proxy since real stores start `nav` at inception) rather than a schema change — revisit if this proves insufficient once real NFOs are scored.
- Confirmed with user: `ScoringEngine.FACTOR_MAP` in `mf_pipeline.py` stays (research-only, backs `mf_factor_backtest.py`'s AUC~0.46 number that Sentinel's `GUARDRAIL_ONLY` alerts cite) — NOT deleted, contrary to the original to-do #3 wording.
- **NEW `mf_live_score.py`** (item #4 + remainder of #1) + wiring into `mf_agent_orchestrator.py` (`MasterOrchestrator._cohort_resources()` lazy instance cache, `evaluate()` calls `score_live()` and appends `cohort_signal` to the result, `print_report()` prints it). Design done by a Fable subagent per G1 (subtle correctness: anti-lookahead at a live anchor, cross-sectional feature scope, train/serve distribution consistency) — see [[mf-architecture-decisions]] for the design writeup. Multi-angle code review before commit caught 4 real issues, all fixed: `_cohort_resources()` only caught `FileNotFoundError`, so any other failure loading the manifest/NAV panel/engine would propagate to `evaluate()`'s outer handler and discard an otherwise-complete evaluation (now catches `Exception`, and the `score_live()` call site has its own try/except too — verified by literally monkeypatching `score_live` to raise and confirming `evaluate()` still returns a full result with a `COHORT SIGNAL NOT EVALUATED — unexpected error` flag); the `score_live()` call omitted `today=TODAY`, so it silently used real wall-clock time instead of the orchestrator's frozen canonical date used everywhere else (fixed); a module-level `_ENGINE_CACHE` keyed by `(id(manifest), id(nav_panel))` in `mf_live_score.py` was a real (if narrow) risk — CPython can reuse a freed object's `id()`, which could silently serve a stale `FeatureEngine` to a differently-loaded caller — removed entirely since `MasterOrchestrator`'s own instance-level cache already provides safe reuse via strong references; missing coverage-flag when the cohort artifact isn't built at all (now added). `mf_agent_orchestrator.py`'s `FundDossier` gained an `amfi_code` field (empty for mock funds, populated by `RealNAVStore.fund()`) as the only new plumbing needed.
- **NEW `mf_artifact.py`** (item #5, batch JSON artifact emitter) + a one-field addition to `mf_sentinel.SentinelReport` (`eligibility`, sourced from the NFO gate status already computed inside `_nfo()`) + a public `MasterOrchestrator.cohort_model_id` property. Runs `MasterOrchestrator(live=True)` over every fund in the universe manifest and serializes facts/sub_scores/verdict/Sentinel-alerts/cohort_signal/eligibility into ONE versioned gzipped JSON. **User decision:** the batch verdict/verdict_color/metric_colors ARE included (reversing the original 2026-07-16 "no verdict in the artifact" design), computed against `InvestorProfile()`'s own default (horizon=7.5y, liquidity=none, risk=high) since the batch run happens before the app knows the real user's profile — every record is stamped `verdict_basis.is_default_profile` + the exact profile used, and the app-side UI requirement to show a "default profile assumed" disclaimer is recorded in `hisaab-kitaab-integration` memory. `monitoring{}` is honestly all-null/zero (prediction ledger, to-do #6, doesn't exist yet); `lock{}` is always null (no free exit-load/lock-in data source anywhere in this pipeline). Verified via `--selftest` on 3 real cached funds (contract shape, gzip round-trip, non-default profile correctly NOT mislabeled as default).
  Multi-angle review before commit caught 4 real issues, all fixed: `sub_scores`/cohort fields were copied into the artifact WITHOUT the same NaN-guard applied to `facts` — since `ProfileRiskScorerAgent` runs unconditionally with no eligibility gate, one NEWBORN/YOUNG fund's NaN sub-score would have hit `json.dumps(..., allow_nan=False)` and raised `ValueError` over the **whole batch write**, discarding every other fund's already-computed record (fixed: every numeric leaf is now NaN-guarded to `None`); `is_default_profile` was hardcoded `True` regardless of which profile was actually passed to `build_artifact()` — a real mislabeling risk if ever called with a bespoke profile (fixed: computed by comparing against `InvestorProfile()`'s own defaults, and selftest now asserts a custom profile is correctly NOT stamped as default); `coverage_flags` (the human-readable DATA GAP strings `RecommendationEngine` already computes) were silently dropped from the artifact even though the module's own docstring claimed they were carried through (fixed: added to the per-fund record); `orch._cohort_inferencer` (a private attribute) was reached into directly from `mf_artifact.py` — replaced with the new public `MasterOrchestrator.cohort_model_id` property. Also simplified: `_load_scheme_names()` now reuses `mf_labels.load_manifest()` instead of a third independent CSV-read of the same file; dropped/errored funds now carry their scheme_name+error reason in `coverage.errors[]`, not just a bare count.
- **NEW `mf_ledger.py`** (item #6, prediction ledger + monitoring). Design by a Fable subagent per G1 (subtle: how to monitor a model whose label needs 3 years to realize without leakage or fabricating precision). Append-only JSONL ledger at git-tracked `ledger/predictions.jsonl` (NOT `mf_cache/`, which is gitignored/regenerable — predictions are the opposite, "unbackfillable" per project decision). Every OK `cohort_signal` this batch run is appended (deduped on `(amfi_code, target, anchor)`); `mf_live_score.py`'s `score_live()` OK result gained `cohort_codes` (frozen cohort peer set) and `features` (the exact scored vector) so a logged prediction can be reproduced/realized years later without redefinition drift. **`interim_ic` renamed `realized_ic`** and stays honestly null (`PENDING_MATURITY`/`INSUFFICIENT_MATURED`) until ~3 real years of matured outcomes exist (~2029 earliest) — Fable's design explicitly rejected computing any shorter-horizon proxy under this name as an honesty-invariant breach (the model was never validated at any horizon but 3y). A separate `rank_stability` QA metric (run-to-run consistency, not a skill claim) was added instead. **PSI drift** computed fresh every run from the live scoring population against an empirical training-decile reference (`mf_cache/phase_b/psi_reference.json`, built via `mf_ledger.py --build-reference` from the cohort model's actual training rows — mean/std-normal reconstruction was rejected since several features are verifiably skewed). Verified via `--selftest`: crash-safe/idempotent append-read, and — the load-bearing proof — a fabricated ledger row at a real historical anchor (2016-03-31) realized against real cached NAV data reproduces `labels.parquet`'s stored `y_cohort_q1` exactly, proving the realization math is correct today without waiting years.
  Ran the built reference against the real 136-fund universe and found `age_years` was the single biggest PSI driver (psi~3.2) — purely mechanical (every fund is simply years older than the pooled 2013-2023 training distribution), not informative drift. Excluded it from `PSI_FEATURES` (23 features now) rather than leave a guaranteed-to-fire, non-actionable signal in place from day one. The current live PSI genuinely shows `SIGNIFICANT_SHIFT` on `mom_12m_ex1m`/`cagr_1y`/`sector_mom_12m` — expected market-regime drift (2025-26 vs the pooled 2013-2023 training era), explicitly anticipated as non-alarming in the design (`psi.status` is "investigate," never an auto-retrain trigger).
  Review agents hit the session's usage limit mid-pass this time; completed the review manually (the code was fresh in context) rather than retrying immediately. Two robustness fixes applied beyond the original design: `read_jsonl`'s torn-tail detection was position-based (only the literal last line) and broke the moment a later append sealed-and-continued past a torn line, permanently misclassifying it as mid-file corruption and raising — fixed to skip any unparseable line unconditionally, regardless of position; the ledger's dedupe-key extraction could `KeyError` on a row missing an expected field (future schema drift) — now degrades to "unmatchable" with a warning instead of crashing the whole append. Also fixed a real bug caught by the artifact selftest itself: `rank_stability`'s "current run" rows were looked up by `run_id` directly from the ledger file, but deduped rows never get re-stamped with a new run's `run_id` — so an ordinary no-op rerun (nothing changed) always found zero "current" rows and starved the metric; fixed by passing the in-memory `ledger_rows` built this run directly, rather than re-deriving them from the file.
- **NEW `mf_holdings.py`** (holdings-into-the-screen, per user re-prioritisation). Pure, data-free, `--selftest`-locked module that reduces a `current_holdings` disclosure snapshot to deterministic structure facts (top1/top5/top10 equity weight, `effective_n`=1/HHI, top-sector weight, cash/debt weight, SEBI single-issuer breaches) + one transparent 0..1 concentration screen metric. Wired into System A **only** (the surface the user chose over an ML feature): (a) a new **SEBI single-issuer 10%-of-NAV rule** in `TrueToLabelVerifier.verify` for EQUITY/HYBRID mandates — index/ETF (OTHER_PASSIVE) naturally exempt — a breach is `critical`, so it rides the existing `critical → SELL` gate (holdings driving the verdict through the compliance channel, a RULE not a laundered score); (b) coverage-gated holdings facts (`top10_weight`/`effective_n`/`top_sector_weight`/`n_holdings`) added to Agent C's `facts` — NaN when no disclosure is on file, so they never colour green from missing data; (c) a `concentration` entry in `RecommendationEngine.metric_colors` (top-10 weight, lower=better, greys out when absent), surfaced in `print_report`. **Honesty stance (enforced in the module docstring + code):** holdings NEVER enter the OOS-validated `cohort_q1` predictor (monthly disclosures can't be reconstructed back to the 2014 training anchors → unbacktestable → would manufacture in-sample accuracy); `concentration` is deliberately **NOT** added to the greens/reds verdict count (a high-conviction book is a legitimate style, not a defect) — only a genuine regulatory breach moves the verdict. No fabricated active-share (free data has no index constituent weights). Verified: `python mf_holdings.py --selftest` (diversified/concentrated/unavailable/empty/debt-only all honest) + a live mock-store evaluation (`Bharat Small Cap`: concentration 🔴 top10=75.5%, single_issuer<=10% critical, verdict SELL) + full `print_report` render. **To activate on a real fund:** drop a disclosure at `mf_cache/disclosures/<amfi_code>_<YYYY-MM>.csv` with columns `instrument,weight,asset_type,sector,cap_band` (RealNAVStore overlays live AMFI cap-bands). ~~NOT YET wired into Sentinel~~ — **now wired into System B too, see below.**
- **Holdings → Sentinel (System B)** — closed the follow-on above. (a) `_compliance_code` now maps `single_issuer<=10%` to a typed **`SEBI_SINGLE_ISSUER_BREACH`** instead of falling through to the `SEBI_OTHER[{rule_id}]` catch-all; severity/basis were already right (it rides `critical` → HIGH/REGULATORY), but the artifact ships alert CODES to the app and a code with a rule_id embedded in the string is not stably branchable. (b) New `_holdings_alerts()` emitting **`HOLDINGS_CONCENTRATION_HIGH`** (WATCH at/above `TOP10_CONCENTRATED`, INFO at/above `TOP10_DIVERSIFIED`, silent below). **Design constraints, all deliberate:** reuses `mf_holdings`' OWN bands rather than parallel thresholds (if they drifted, the app could show a green concentration metric beside a concentration WATCH on the same fund); basis is `DESCRIPTIVE_RISK` so `_NEVER_HIGH` structurally caps it below HIGH — a concentrated book is a legitimate high-conviction style, and only the real SEBI breach may reach the push-notification tier; EQUITY/HYBRID only, mirroring the single-issuer exemption, since an index fund's top-10 weight is a property of the INDEX, not a manager decision (those buckets are mandate-inapplicable, so not reported dormant either); **NOT gated on NFO eligibility** — concentration needs a disclosure, not NAV history, so it is often the only evidence available on a newborn fund whose factor rules are all dormant; no disclosure → dormant entry, never an alert and never a silent pass. Verified: `mf_sentinel.py --selftest` (concentrated→WATCH, diversified silent, no-disclosure dormant, passive exempt, DESCRIPTIVE_RISK never HIGH, single-issuer typed) + end-to-end on mock data (Bharat Small Cap WATCH at top10=75.5% with single-issuer HIGH; Nifty 50 index fund and Solution-Oriented legacy fund correctly silent) + end-to-end on real data (Parag Parikh Flexi Cap, no disclosure → no alert, dormant entry, grey metric).
- **Known limitation (not introduced here, shared with System A):** `top10_weight` is a fraction of **NAV**, not of the equity sleeve, so a conservative hybrid with a small equity allocation can never reach the 0.45/0.65 bands even if its equity sleeve is highly concentrated. Left as-is deliberately — re-banding only System B would break the A/B consistency the design depends on. `effective_n` in the alert evidence does capture sleeve-level concentration if this needs revisiting.
- **Batch-write fragility — FOUND then FIXED** (`125151f`). `mf_artifact.py` copied each alert's `evidence` dict verbatim into a `json.dumps(..., allow_nan=False)` write, so one non-finite float in ANY alert's evidence raised over the **whole batch**, discarding every other fund's already-computed record — the same failure mode already fixed for `sub_scores`, never extended to the nested payloads. Nothing shipped triggered it (every author routes floats through `mf_sentinel._fmt()`), but nothing enforced that convention either. New `_json_safe()` recursively guards the three payloads this module doesn't enumerate leaf-by-leaf — alert `evidence`, `nfo_dossier`, `signal_context` — making the guarantee structural rather than conventional. Two deliberate subtleties: **bools pass through untouched** (`isinstance(True, int)` is True in Python, so a naive numeric branch would rewrite `True` as `1.0` and corrupt the contract), and **numpy scalars are unwrapped via `.item()`** (`np.float64` subclasses `float` and was already covered, but `np.float32`/`np.int32` are not and `json.dumps` rejects them outright as "not JSON serializable" — the same batch-wide failure by a different exception). Selftest feeds NaN/Inf/np.float32-NaN/np.float32/np.int32/nested-bool through the REAL `build_fund_record` path; confirmed the test has teeth (same payload without the guard raises).
- **NEW `mf_eval.py`** (model-health evaluation framework — "when do I tweak the model?"). Pure, data-free, `--selftest`-locked decision layer on top of `mf_ledger.py`: grades the raw monitoring signals (PSI, rank_stability, coverage, matured-outcome lift) against **documented GREEN/AMBER/RED thresholds** (tunable constants at the top of the module) into a per-metric `MetricHealth` + an overall model-health status + an app-renderable panel (`render()`). Adds `outcome_metrics()` (extends `realized_summary` with realized lift/precision/calibration on **matured** predictions). **Honesty enforced in code + selftest:** (1) `outcome_skill` is the real "tweak it" signal but stays **PENDING** until ~50 predictions mature (~2029) — a PENDING outcome NEVER reddens the overall status ("not-yet-measurable" ≠ "bad"); (2) PSI drift caps at **AMBER** (investigate, never an auto-retrain RED — matches the ledger design note); (3) only a matured-outcome collapse (lift <1.0, worse than chance) turns the model RED; (4) grades the `cohort_q1` STATISTICAL model only — deliberately NOT the deterministic BUY/HOLD/SELL rule (that would relaunder the retired composite). Verified: `python mf_eval.py --selftest` (healthy+pending→GREEN, PSI significant→AMBER, erratic ranks+errors→RED, below-chance matured outcome→RED, stale reference→UNAVAILABLE).
- **Wired into the app artifact** (`mf_artifact.py`): new top-level **`evaluation{}`** block (status/headline/metrics[]/outcome/disclaimer), computed every batch run via `mf_eval.build_report_from_ledger`. `build_artifact` gained a `realizations_path` param (threaded through, selftest kept hermetic with a tmp path). Artifact CONTRACT docstring + selftest updated (asserts outcome_skill PENDING and that a pending outcome never reddens the panel). **App-side contract documented** in `docs/app_evaluation_contract.md` — schema, the four metrics + thresholds, the mandatory "health ≠ accuracy" disclaimer the UI must show, and suggested rendering. Mirror the UI decisions into Hisaab Kitaab `DECISIONS.md` when integration starts.
- Hisaab Kitaab `DECISIONS.md`: `f99fa96` (integration contract) + `9885565` (verdict amendment).

## To-do (ordered; resume at #8 — #7 is built but its CI has NEVER run; schedules are paused pending one manual `workflow_dispatch` verification from the Actions tab, then uncomment the crons in both workflows)
1. ✅ Serialize cohort inference payload (`b44a029`) + wire `cohort_q1` percentile into System A's output (`mf_live_score.py`, this session) — DONE, no longer blocked.
2. ✅ Kill composite → honest SCREEN + tri-colour verdict (`31d42a2`, `29a633a`).
3. ✅ **Sentinel alert engine built + wired** (`mf_sentinel.py` new, wired into `MasterOrchestrator`). Typed alert registry (compliance breach=HIGH; factor thresholds e.g. downside_capture>1.15, drawdown-vs-COVID, expense outlier; manager MACS/tenure red flags — each citing evidence values), `NFOAssessor` dossier wired as B's output including manager cross-fund proxy lookup from `mf_cache/managers.csv`. Alert severity thresholds shipped as sensible defaults (documented as tunable constants at the top of `mf_sentinel.py`) — proceeded per user's earlier call, still open to revisit. `FACTOR_MAP` in `mf_pipeline.ScoringEngine` kept (research-only backtest infra), not deleted — see product-shape note above.
4. ✅ **Latest-anchor live-scoring path built + wired** (`mf_live_score.py`, this session). ~100x fewer anchors touched (1 vs 144); universe-wide panel, not cohort-restricted (see product-shape note). Unblocked #1's cohort wiring.
5. ✅ **Artifact emitter built** (`mf_artifact.py`, this session). Versioned gzipped JSON; per-fund verdict/verdict_color/metric_colors/cohort_signal, all stamped `is_default_profile`. `monitoring{}` honestly null pre-ledger.
6. ✅ Prediction ledger + monitoring built + wired (`mf_ledger.py`, this session). Git-tracked `ledger/predictions.jsonl`; `realized_ic` honestly null until ~2029; PSI computed fresh each run; `rank_stability` QA proxy added. Ledger now exists BEFORE any deploy, as required.
7. ✅ **Scheduler built** (`.github/workflows/nightly.yml` + `realize-monthly.yml`, $0). Nightly: restore `mf_cache/` via `actions/cache` (evolving-cache pattern) → pure selftests gate → `bootstrap.py --from-manifest` (NEW flag: refresh the whole tracked universe; throttle = cache-first 20h TTL, no sleep) → model selftests if the cohort artifact is present → `mf_artifact.py` (emit + append ledger) → commit `ledger/` back (git-tracked/unbackfillable) → upload artifact. Monthly: `mf_ledger.py --realize` (offline maturation join) + commit realizations. No secrets (all free no-key APIs; Agent D degrades to NEUTRAL). Cold-cache seeding + app-consumption (workflow artifact now; GitHub Release for a stable app URL) documented in `docs/ci.md`. Both workflows YAML-validated. True NAVAll incremental-append left as a future optimization (TTL already prevents redundant refetch).
8. ✅ **Phase 2 built, RE-SCOPED with user approval** (`a7f4091`, `03260ec`, `24468fb`, `5985e26`, `4fed219`, `849752c`). Literal "whole-market scoring" would breach the honesty invariant: the trained universe is 136 funds / 20 cohorts / **10 categories, all EQUITY**, while NAVAll's 14,202 rows are mostly debt/index/FoF/ETF — emitting a probability there claims the holdout AUC (~0.578) transfers to populations the model never saw. Delivered instead as **"screen wide, gate the model"**:
   - NEW **`mf_universe.py`** — AMFI `category_raw` → canonical category + trained-universe gate. Trained set read from the LIVE manifest, never hardcoded (asserted to widen *and* narrow with it).
   - NEW **`mf_overrides.py`** + git-tracked **`overrides/`** — hand-curated `category`/`sector` the pipeline cannot fetch. INPUTS ONLY; any output-setting column (label/probability/verdict/…) rejects the whole file.
   - `mf_live_score.py` gained `OUT_OF_TRAINING_UNIVERSE` (permanent refusal, distinct from the fixable `NOT_IN_UNIVERSE`), `SECTOR_UNRESOLVED`, and **frozen-panel insertion scoring** for funds in a trained category but outside the 136.
   - Real numbers: **569** canonical Direct+Growth scoreable schemes vs 136 today; `UNMAPPED_CATEGORY` **0** of 14,202; **433** outside the manifest of which **229 scoreable now** and **204 blocked on a curated sector**.
8b. ✅ **Artifact coverage flags caught up with #8** (`9878a7e`, 2026-08-03). `mf_artifact._data_flags()` predated Phase 2: `OUT_OF_TRAINING_UNIVERSE` and `SECTOR_UNRESOLVED` emitted **no flag at all** (a permanently-refused fund looked identical to a cleanly-scored one), and `NOT_IN_UNIVERSE` was mapped onto the flag *named* `OUT_OF_TRAINING_UNIVERSE` — opposite meanings since #8, and the app branches on these codes. Replaced the if/elif chain with an exhaustive `_COHORT_STATUS_FLAG` mapping whose coverage of `mf_live_score._STATUSES` is selftest-asserted (that chain is *how* they drifted); unknown statuses degrade to a stable generic code, never an interpolated one. `signal_a` gained **`cohort_status`** — nothing else in the record said why `cohort_q1_prob` was null. `coverage{}` counts the three refusals separately (fetch backlog / permanent / curation worklist) and they stay out of `mf_eval`'s error rate, since a refusal is correct behaviour rather than a pipeline failure.

9. DEFERRED/GATED: 1y-forward cohort head (only if it clears CPCV >0.5 post phase-2).
10. **NOT DONE — the last gate before anything reaches the app: the CI has NEVER run.** Both crons in `.github/workflows/` are commented out pending one manual `workflow_dispatch` from the Actions tab. Needs the repo owner (no `gh` CLI on the dev box).

**Then:** Phase C (evaluate NFOs JM Multi Asset + TRUSTMF Large & Mid via B's dossier) → Step 3 (manager-proxy dossiers, subsumed into NFOAssessor) → capstone (layman summary + retrospective + integration plan). App integration itself still deferred.

## Known gaps / blockers (as of `849752c`)
Ordered by what actually blocks progress, with who can clear it.

1. **CI has never executed** (needs repo owner). Crons commented out in both workflows; run `nightly-refresh` once via **Run workflow**, confirm green, then uncomment the two `schedule:` lines in each. Everything downstream is unverified until this passes once.
2. **204 funds blocked on a curated sector** (needs a human + sources). `Sectoral/Thematic` cohorts key on `("sector", …)`; AMFI publishes no sector field and the manifest's 52 sectors are hand-typed. Workflow: `mf_overrides.py --gaps` → fill rows → `mf_overrides.py --validate`. Not fixable by fetching.
3. ✅ **RESOLVED — the universe manifest is now git-tracked** at `overrides/universe_manifest.csv` (was `mf_cache/universe_manifest.csv`, gitignored + sitting in the directory CI restores from an evictable cache, with 52 unbackfillable hand-typed sectors in it). `mf_labels.MANIFEST_PATH` is now the single definition of the path and `mf_realstore.UNIVERSE_MANIFEST` aliases it rather than keeping a second literal. Side benefit: `bootstrap.py --from-manifest` now works on a **cold** CI runner, since the manifest arrives with the checkout — the workflow's cold-cache caveat now applies only to the cohort model artifact.
4. **NAV coverage: ~198 cached vs 569 scoreable — NOT mechanical, premise corrected 2026-08-03.** This was listed as "just a `bootstrap.py` run". It is not, for two independent reasons, both verified against the code:
   - `bootstrap.py --from-manifest` loads its fund list from `load_manifest()["scheme_name"]` — the **136 manifest funds only**. There is no flag that fetches the 433 out-of-manifest canonical schemes.
   - `mf_artifact._load_scheme_names()` also reads the manifest, so the **batch artifact covers 136 funds regardless of what is cached**. Phase 2's insertion scoring is built and verified, but nothing in the batch path ever calls it — the 313 scoreable funds never reach the app.

   So widening coverage is a **design change to the batch loop** (iterate `mf_universe`'s canonical scoreable set instead of the manifest, insertion-score the outsiders, decide what a non-manifest fund's record looks like), not a fetch. `nav_loader` fetching a candidate on demand at score time is real and already wired — it just never fires, because nothing enumerates candidates. **Needs a product call before it is built:** does the artifact ship 136 funds or 313?
5. **`mf_cache/managers.csv` does not exist**, so Phase C's NFO manager-proxy dossier — the entire point of Phase C — has nothing to read. Schema: `manager_name, amfi_code, scheme_name, start_date, end_date, source_url`, every row traceable to a real AMC factsheet/SID.
6. **`mf_cache/disclosures/` does not exist**, so holdings are inert on every real fund (concentration greys out, SEBI single-issuer check reports NOT EVALUATED). The code is proven on mock data. Drop `<amfi_code>_<YYYY-MM>.csv` files to activate.
7. **`/code-review` skill is unusable in this repo** — it resolves its argument as a GitHub PR via `gh`, which is not installed on the dev box. Review the local diff manually (that is what the recent commits did).
8. **Fable is unavailable** (`Fable 5 requires usage credits`). G1 says flag Fable-level work — expect the spawn to fail. Item #8 was designed on Opus instead and that was sufficient; what mattered was verifying premises against code, not model tier.

## Env / workflow
- Python: venv only — `./.venv/Scripts/python.exe`. Bare `python`/`pip` are broken stubs.
- `PYTHONIOENCODING=utf-8` for anything printing ₹ / emoji.
- Commit per step to `master` after `/code-review` + `/security-review` (`origin/HEAD` now resolves → the skill's auto-target works; the old manual-pass workaround is obsolete). Pushing remains an explicit, separate step. Co-Author trailer required.
- `mf_cache/` is gitignored (data + model artifacts are generated; regenerate via `python mf_model.py --stage cohort`).
